<h1>
  <%= t('comfy_blog.comments') %>
  <% if @post %>
    <%= t('comfy_blog.for') %> <%= @post.title %>
  <% end %>
</h1>

<ul class='list comments'>
  <% @comments.each do |comment| %>
    <%= render :partial => 'comment', :object => comment %>
  <% end %>
</ul>
